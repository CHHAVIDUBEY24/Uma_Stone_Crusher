# Uma Stone Crusher — placeholder website

This branch `add-website` contains a small responsive static site with placeholder images and content. Replace the placeholders in `assets/images/` with real photos, and update contact details in `index.html`.

Files added:
- index.html
- css/styles.css
- assets/script.js
- assets/images/* (svg placeholders)

How to update photos:
- Replace `assets/images/gravel1.svg`, `gravel2.svg`, `gravel3.svg` with your gravel photos (keep filenames or update `index.html`).
- Replace owner photos in `assets/images/owner_main.svg`, `owner1.svg`, `owner2.svg`, `owner3.svg`.

Next steps:
- Tell me the real text, phone numbers, and upload photos and I will update the site.
- If you want the site deployed via GitHub Pages, tell me which branch should be used for Pages.
